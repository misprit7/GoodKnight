# Notes

* Because of dynamic imports intellisense can't find `*.theme.less` files. 
* Because of React's rendering, the `DynamicTheme` component has to be declared as a `memo` or else there are major performance losses